{
  "packages": ["test/data"],
  "commands": {
    "setUp": ["npm run setup"],
    "install": ["npm install"],
    "test": ["npm run test"],
    "build": ["npm run build"],
    "afterBuild": [],
    "publish": ["npm publish"],
    "canaryPublish": ["npm publish --tag canary"],
    "tryPublish": ["npm publish --dry-run"]
  },
  "packageCommands": {}
}
